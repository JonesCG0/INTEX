# Increase proxy buffer sizes to limit writes to /var/lib/nginx/tmp/proxy.
proxy_buffer_size   64k;
proxy_buffers       8 128k;
proxy_busy_buffers_size 256k;

# Keep large upstream responses in memory instead of proxy temp files.
proxy_max_temp_file_size 0;
