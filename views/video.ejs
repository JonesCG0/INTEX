<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Video Page</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light p-4">

<div class="container" style="max-width: 700px;">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Watch the Video</h2>
    <div>
      <a href="/" class="btn btn-secondary btn-sm me-2">Home</a>
      <% if (user) { %>
        <a href="/users" class="btn btn-primary btn-sm me-2">Users</a>
        <a href="/auth/logout" class="btn btn-danger btn-sm">Logout</a>
      <% } else { %>
        <a href="/auth/login" class="btn btn-outline-primary btn-sm">Login</a>
      <% } %>
    </div>
  </div>

  <p class="mb-3">
    Click the button below to open the YouTube video in a new tab:
  </p>

  <a href="<%= youtubeUrl %>" target="_blank" class="btn btn-danger btn-lg">
    📺 Watch on YouTube
  </a>

  <hr class="my-4">

  <p class="text-muted">
    You can customize this page with more info, instructions, or embedded content if you want.
  </p>
</div>

<script src="/teapot.js"></script>
</body>
</html>
