<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Event Details</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container">
    <h1>Event Details</h1>

    <div class="details">
      <p><strong>ID:</strong> <%= event.eventid %></p>
      <p><strong>Title:</strong> <%= event.title || '—' %></p>
      <p><strong>Type:</strong> <%= event.eventtype || '—' %></p>
      <p><strong>Start:</strong> <%= event.date || '—' %></p>
      <p><strong>End:</strong> <%= event.eventdatetimeend || '—' %></p>
      <p><strong>Location:</strong> <%= event.eventlocation || '—' %></p>
      <p><strong>Capacity:</strong> <%= event.eventcapacity || '—' %></p>
      <p><strong>Registration Deadline:</strong> <%= event.eventregistrationdeadline || '—' %></p>
      <p><strong>Recurrence Pattern:</strong> <%= event.eventrecurrencepattern || '—' %></p>
      <p><strong>Default Capacity:</strong> <%= event.eventdefaultcapacity || '—' %></p>
      <p><strong>Description:</strong> <%= event.eventdescription || '—' %></p>
    </div>

    <a href="/events/<%= event.eventid %>/edit" class="btn">Edit</a>
    <a href="/events" class="btn btn-secondary">Back to List</a>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
