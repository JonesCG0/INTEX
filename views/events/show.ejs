<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Event Details</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container">
    <h1>Event Details</h1>

    <div class="details">
      <p><strong>ID:</strong> <%= event.eventid %></p>
      <p><strong>Title:</strong> <%= event.title || 'N/A' %></p>
      <p><strong>Date:</strong> <%= event.date || 'N/A' %></p>
      <!-- Add more event fields as needed -->
    </div>

    <a href="/events/<%= event.eventid %>/edit" class="btn">Edit</a>
    <a href="/events" class="btn btn-secondary">Back to List</a>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
