<%- include('../partials/header', { title: 'Milestones', user: user }) %>

<div class="page-header">
  <div class="container">
    <h1><i class="bi bi-flag-fill"></i> Milestones</h1>
    <p>Track participant achievements and milestones</p>
  </div>
</div>

<div class="container">
  <div class="card">
    <% if (user && user.role === 'admin') { %>
    <div class="mb-3">
      <a href="/milestones/new" class="btn btn-success me-2">
        <i class="bi bi-plus-circle-fill"></i> Add New Milestone
      </a>
    </div>
    <% } %>

    <div
      class="d-flex flex-column flex-lg-row align-items-lg-center gap-2 mb-3"
    >
      <div class="d-flex align-items-center gap-2 flex-grow-1 flex-lg-grow-0">
        <input
          type="search"
          class="form-control w-auto"
          style="min-width: 240px; max-width: 320px"
          placeholder="Search milestones"
          aria-label="Search milestones"
          data-search-for="milestones-table"
        />
        <nav aria-label="Milestones pagination (top)">
          <ul
            class="pagination pagination-sm mb-0 pagination-soft"
            data-pagination-for="milestones-table"
          ></ul>
        </nav>
      </div>
      <div class="d-flex align-items-center gap-2 ms-lg-auto">
        <label for="milestones-page-size" class="text-muted small mb-0"
          >Rows/page</label
        >
        <select
          id="milestones-page-size"
          class="form-select form-select-sm w-auto"
          data-page-size-for="milestones-table"
        >
          <option value="10">10</option>
          <option value="20" selected>20</option>
          <option value="30">30</option>
          <option value="40">40</option>
          <option value="50">50</option>
        </select>
      </div>
    </div>

    <div class="table-responsive">
      <table class="table" data-table-id="milestones-table">
        <thead>
          <tr>
            <th>
              <a
                href="?sortBy=milestonetitle&sortOrder=<%= sortBy === 'milestonetitle' && sortOrder === 'asc' ? 'desc' : 'asc' %>"
                style="
                  color: inherit;
                  text-decoration: none;
                  cursor: pointer;
                  white-space: nowrap;
                "
              >
                Title <% if (sortBy === 'milestonetitle') { %>
                <i
                  class="bi bi-arrow-<%= sortOrder === 'asc' ? 'up' : 'down' %>"
                ></i>
                <% } else { %>
                <i
                  class="bi bi-arrow-down-up"
                  style="opacity: 0.3; font-size: 0.8em"
                ></i>
                <% } %>
              </a>
            </th>
            <th>
              <a
                href="?sortBy=milestonedate&sortOrder=<%= sortBy === 'milestonedate' && sortOrder === 'asc' ? 'desc' : 'asc' %>"
                style="
                  color: inherit;
                  text-decoration: none;
                  cursor: pointer;
                  white-space: nowrap;
                "
              >
                Date <% if (sortBy === 'milestonedate') { %>
                <i
                  class="bi bi-arrow-<%= sortOrder === 'asc' ? 'up' : 'down' %>"
                ></i>
                <% } else { %>
                <i
                  class="bi bi-arrow-down-up"
                  style="opacity: 0.3; font-size: 0.8em"
                ></i>
                <% } %>
              </a>
            </th>
            <th>
              <a
                href="?sortBy=userfirstname&sortOrder=<%= sortBy === 'userfirstname' && sortOrder === 'asc' ? 'desc' : 'asc' %>"
                style="
                  color: inherit;
                  text-decoration: none;
                  cursor: pointer;
                  white-space: nowrap;
                "
              >
                Participant Name <% if (sortBy === 'userfirstname') { %>
                <i
                  class="bi bi-arrow-<%= sortOrder === 'asc' ? 'up' : 'down' %>"
                ></i>
                <% } else { %>
                <i
                  class="bi bi-arrow-down-up"
                  style="opacity: 0.3; font-size: 0.8em"
                ></i>
                <% } %>
              </a>
            </th>
          </tr>
        </thead>
        <tbody>
          <% milestones.forEach(m => { %>
          <tr>
            <td><strong><%= m.milestonetitle || '—' %></strong></td>
            <td>
              <% if (m.milestonedate) { %> <%= new
              Date(m.milestonedate).toLocaleString('en-US', { year: 'numeric',
              month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit'
              }) %> <% } else { %> — <% } %>
            </td>
            <td><%= m.userfirstname || '—' %> <%= m.userlastname || '—' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <div class="d-flex justify-content-center mt-3">
      <nav aria-label="Milestones pagination">
        <ul
          class="pagination pagination-sm mb-0 pagination-soft"
          data-pagination-for="milestones-table"
        ></ul>
      </nav>
    </div>
  </div>
</div>

<%- include('../partials/footer-scripts') %>
