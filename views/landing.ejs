<%- include('partials/header', { title: 'Ella Rises - Home', user: user }) %>

</div> <!-- Close main-content opened by header -->

<style>
  .hero-section {
    padding: 30px 0 80px 0;
    color: #3a3f3b;
    position: relative;
    overflow: hidden;
    margin-top: 0;
  }

  .hero-section::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url('/Mural_072.jpg');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    opacity: 0.6;
    z-index: 0;
  }

  .hero-section::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, #ffd8d1, #f9afb1);
    opacity: .4;
    z-index: 1;
  }

  .hero-section .container {
    position: relative;
    z-index: 2;
  }

  .hero-section h1 {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 20px;
    line-height: 1.2;
  }

  .hero-section p {
    font-size: 1.4rem;
    margin-bottom: 30px;
    font-weight: 600;
    opacity: 0.95;
  }

  .btn-custom-primary {
    background-color: #ffffff;
    color: #d4654c;
    border: none;
    padding: 14px 35px;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 50px;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
  }

  .btn-custom-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.25);
    background-color: #faf8f5;
    color: #d4654c;
  }

  .stats-section {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-radius: 20px;
    padding: 40px;
    margin-top: 50px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
  }

  .stat-item {
    text-align: center;
    padding: 20px;
  }

  .stat-item i {
    font-size: 3rem;
    color: #d4654c;
    margin-bottom: 15px;
  }

  .stat-item h2 {
    font-size: 2.5rem;
    font-weight: 700;
    color: #d4654c;
    margin: 10px 0;
    min-height: 60px;
  }

  .stat-item h3 {
    font-size: 1.1rem;
    font-weight: 600;
    color: #2c2c2c;
    margin: 0;
  }

  .features-section {
    padding: 80px 0;
    background-color: #faf8f5;
  }

  .section-title {
    text-align: center;
    margin-bottom: 60px;
  }

  .section-title h2 {
    font-size: 2.5rem;
    font-weight: 700;
    color: #2c2c2c;
    margin-bottom: 15px;
  }

  .section-title p {
    font-size: 1.1rem;
    color: #8b6f47;
    font-weight: 400;
  }

  .feature-card {
    background: #ffffff;
    border-radius: 15px;
    padding: 0;
    margin-bottom: 30px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
    transition: all 0.3s ease;
    height: 100%;
    border: 2px solid transparent;
    overflow: hidden;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 8px 30px rgba(212, 101, 76, 0.15);
    border-color: #e8a87c;
  }

  .feature-card img {
    width: 100%;
    height: 250px;
    object-fit: cover;
    display: block;
  }

  .features-section a {
    text-decoration: none;
  }

  .feature-card h3 {
    color: #2c2c2c;
    font-size: 1.4rem;
    font-weight: 600;
    margin: 0;
    padding: 20px;
    text-align: center;
  }

  @media (max-width: 768px) {
    .hero-section h1 {
      font-size: 2rem;
    }
    .hero-section p {
      font-size: 1rem;
    }
    .stat-item {
      margin-bottom: 20px;
    }
  }

  /* Supporters Section */
  .supporters-section {
    background-color: rgb(253, 216, 209);
    padding: 80px 0;
    text-align: center;
  }

  .supporters-section h2 {
    font-size: 2.5rem;
    font-weight: 700;
    color: #2c2c2c;
    margin-bottom: 20px;
  }

  .supporters-section p {
    font-size: 1.1rem;
    color: #2c2c2c;
    margin-bottom: 10px;
    line-height: 1.6;
  }

  .supporters-section .highlight {
    font-weight: 600;
    color: #2c2c2c;
  }

  /* Logo Loop Container */
  .logo-loop-container {
    width: 100%;
    overflow: hidden;
    margin-top: 60px;
    position: relative;
    padding: 20px 0;
  }

  .logo-loop-container::before,
  .logo-loop-container::after {
    content: "";
    position: absolute;
    top: 0;
    width: 100px;
    height: 100%;
    z-index: 2;
    pointer-events: none;
  }

  .logo-loop-container::before {
    left: 0;
    background: linear-gradient(to right, rgba(253, 216, 209, 1), rgba(255, 219, 219, 0));
  }

  .logo-loop-container::after {
    right: 0;
    background: linear-gradient(to left, rgba(253, 216, 209, 1), rgba(255, 219, 219, 0));
  }

  .logo-loop-track {
    display: flex;
    gap: 30px;
    animation: scroll-left 40s linear infinite;
    width: fit-content;
  }

  /* .logo-loop-track:hover {
    animation-play-state: paused;
  } */

  .logo-item {
    flex-shrink: 0;
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 20px;
  }

  .logo-item img {
    max-height: 100px;
    max-width: 200px;
    width: auto;
    height: auto;
    object-fit: contain;
    filter: grayscale(0%);
    opacity: 1;
    transition: all 0.3s ease;
  }

  /* .logo-item:hover img {
    filter: grayscale(0%);
    opacity: 1;
    transform: scale(1.1);
  } */

  @keyframes scroll-left {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  @media (max-width: 768px) {
    .logo-loop-track {
      gap: 0px;
    }

    .logo-item {
      height: 60px;
      padding: 0 15px;
    }

    .logo-item img {
      max-height: 60px;
      max-width: 150px;
    }
  }
</style>

<!-- Hero Section -->
<div class="hero-section">
  <div class="container">
    <div class="row align-items-center">
      <div class="col-lg-12 text-center">
        <% if (user) { %>
          <h1>Welcome Back, <%= user.userfirstname %>!</h1>
          <img src="/Ella_Rises_Logo.avif" alt="Ella Rises Logo" />
          <p>
Inviting young women to pursue higher education and embrace their heritage by providing culturally rooted programs and educational experiences.</p>
          <% if (user.role === 'admin') { %>
          <a href="/dashboard" class="btn btn-custom-primary">
            <i class="bi bi-speedometer2"></i> Go to Dashboard
          </a>
          <% }%>
        <% } else { %>
          <h1>Welcome to</h1>
          <img src="/Ella_Rises_Logo.avif" alt="Ella Rises Logo" />
          <p>
Inviting young women to pursue higher education and embrace their heritage by providing culturally rooted programs and educational experiences.</p>
          <a href="/auth/login" class="btn btn-custom-primary">
            <i class="bi bi-box-arrow-in-right"></i> Get Started
          </a>
        <% } %>

        <!-- Stats Section -->
        <% if (user) { %>
          <div class="stats-section">
            <div class="row">
              <div class="col-md-3 col-6 stat-item">
                <i class="bi bi-people-fill"></i>
                <h2><%= stats.participants %></h2>
                <h3>Participants</h3>
              </div>
              <div class="col-md-3 col-6 stat-item">
                <i class="bi bi-calendar-event"></i>
                <h2><%= stats.events %></h2>
                <h3>Events</h3>
              </div>
              <div class="col-md-3 col-6 stat-item">
                <i class="bi bi-flag-fill"></i>
                <h2><%= stats.milestones %></h2>
                <h3>Milestones</h3>
              </div>
              <div class="col-md-3 col-6 stat-item">
                <i class="bi bi-currency-dollar"></i>
                <h2>$<%= Number(stats.donations).toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %></h2>
                <h3>Donations</h3>
              </div>
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</div>

<!-- Programs Section -->
<div class="features-section">
  <div class="container">
    <div class="section-title">
      <h2>Programs</h2>
    </div>

    <div class="row">
      <div class="col-lg-3 col-md-6">
        <a href="https://www.ellarises.org/programs">
        <div class="feature-card">
          <img src="/Ballet_Folkorico.png" alt="Ballet Folklorico" href="https://www.ellarises.org/programs">
          <h3>Ballet Folklorico</h3>
        </div>
        </a>
      </div>
        <div class="col-lg-3 col-md-6">
          <a href="https://www.ellarises.org/programs">
          <div class="feature-card">
            <img src="/Mariachi.png" alt="Mariachi" >
            <h3>Mariachi</h3>
          </div>
          </a>
        </div>
    
      
      <div class="col-lg-3 col-md-6">
        <a href="https://www.ellarises.org/programs">
        <div class="feature-card">
          <img src="/STEAM.png" alt="STEAM" href="https://www.ellarises.org/programs">
          <h3>STEAM</h3>
        </div>
        </a>
      </div>
      <div class="col-lg-3 col-md-6">
        <a href="https://www.ellarises.org/programs">
        <div class="feature-card">
          <img src="/Summer_Summit.png" alt="Summer Summit" href="https://www.ellarises.org/programs">
          <h3>Summer Summit</h3>
        </div>
        </a>
      </div>
    </div>
  </div>
</div>

<!-- Supporters Section -->
<div class="supporters-section">
  <div class="container">
    <h2>THANK YOU</h2>
    <p>A big thank you to our supporters who make it possible for us to accomplish our</p>
    <p>goals and provide these wonderful programs for our young women!</p>
    <p class="highlight">Your generous contributions have lasting impact.</p>

    <div class="logo-loop-container">
      <div class="logo-loop-track">
        <!-- First set of logos -->
        <div class="logo-item"><img src="/sponsor_logos/100Women.png" alt="100 Women"></div>
        <div class="logo-item"><img src="/sponsor_logos/BYU.png" alt="BYU"></div>
        <div class="logo-item"><img src="/sponsor_logos/Community_Food_Bank.png" alt="Community Food Bank"></div>
        <div class="logo-item"><img src="/sponsor_logos/DepofCulturalEngagement.png" alt="Department of Cultural Engagement"></div>
        <div class="logo-item"><img src="/sponsor_logos/MTech.png" alt="MTech"></div>
        <div class="logo-item"><img src="/sponsor_logos/Meta.png" alt="Meta"></div>
        <div class="logo-item"><img src="/sponsor_logos/Minky.png" alt="Minky"></div>
        <div class="logo-item"><img src="/sponsor_logos/MountainAmerica.png" alt="Mountain America"></div>
        <div class="logo-item"><img src="/sponsor_logos/ParksAndRec.png" alt="Parks and Recreation"></div>
        <div class="logo-item"><img src="/sponsor_logos/Prolook.png" alt="Prolook"></div>
        <div class="logo-item"><img src="/sponsor_logos/ProvoSchoolDistrict.png" alt="Provo School District"></div>
        <div class="logo-item"><img src="/sponsor_logos/TPoint.png" alt="Thanksgiving Point"></div>
        <div class="logo-item"><img src="/sponsor_logos/UVU.png" alt="UVU"></div>
        <div class="logo-item"><img src="/sponsor_logos/UnitedWay.png" alt="United Way"></div>
        <div class="logo-item"><img src="/sponsor_logos/UofU.png" alt="University of Utah"></div>
        <div class="logo-item"><img src="/sponsor_logos/UtahW&L.png" alt="Utah Women & Leadership"></div>

        <!-- Duplicate set for seamless loop -->
        <div class="logo-item"><img src="/sponsor_logos/100Women.png" alt="100 Women"></div>
        <div class="logo-item"><img src="/sponsor_logos/BYU.png" alt="BYU"></div>
        <div class="logo-item"><img src="/sponsor_logos/Community_Food_Bank.png" alt="Community Food Bank"></div>
        <div class="logo-item"><img src="/sponsor_logos/DepofCulturalEngagement.png" alt="Department of Cultural Engagement"></div>
        <div class="logo-item"><img src="/sponsor_logos/MTech.png" alt="MTech"></div>
        <div class="logo-item"><img src="/sponsor_logos/Meta.png" alt="Meta"></div>
        <div class="logo-item"><img src="/sponsor_logos/Minky.png" alt="Minky"></div>
        <div class="logo-item"><img src="/sponsor_logos/MountainAmerica.png" alt="Mountain America"></div>
        <div class="logo-item"><img src="/sponsor_logos/ParksAndRec.png" alt="Parks and Recreation"></div>
        <div class="logo-item"><img src="/sponsor_logos/Prolook.png" alt="Prolook"></div>
        <div class="logo-item"><img src="/sponsor_logos/ProvoSchoolDistrict.png" alt="Provo School District"></div>
        <div class="logo-item"><img src="/sponsor_logos/TPoint.png" alt="Thanksgiving Point"></div>
        <div class="logo-item"><img src="/sponsor_logos/UVU.png" alt="UVU"></div>
        <div class="logo-item"><img src="/sponsor_logos/UnitedWay.png" alt="United Way"></div>
        <div class="logo-item"><img src="/sponsor_logos/UofU.png" alt="University of Utah"></div>
        <div class="logo-item"><img src="/sponsor_logos/UtahW&L.png" alt="Utah Women & Leadership"></div>
      </div>
    </div>
  </div>
</div>

<script>
  // Count-up animation for stats
  document.addEventListener('DOMContentLoaded', function() {
    const stats = document.querySelectorAll('.stat-item h2');
    let hasAnimated = false;

    function easeOutCubic(t) {
      return 1 - Math.pow(1 - t, 3);
    }

    function animateValue(element, start, end, duration, isDecimal = false) {
      const startTime = performance.now();
      const isDollar = element.textContent.includes('$');

      function update(currentTime) {
        const elapsed = currentTime - startTime;
        const progress = Math.min(elapsed / duration, 1);
        const easedProgress = easeOutCubic(progress);
        const current = start + (end - start) * easedProgress;

        if (isDollar) {
          element.textContent = '$' + current.toLocaleString('en-US', {
            minimumFractionDigits: 2,
            maximumFractionDigits: 2
          });
        } else {
          element.textContent = Math.floor(current).toLocaleString('en-US');
        }

        if (progress < 1) {
          requestAnimationFrame(update);
        } else {
          // Ensure final value is exact
          if (isDollar) {
            element.textContent = '$' + end.toLocaleString('en-US', {
              minimumFractionDigits: 2,
              maximumFractionDigits: 2
            });
          } else {
            element.textContent = Math.floor(end).toLocaleString('en-US');
          }
        }
      }

      requestAnimationFrame(update);
    }

    function startAnimation() {
      stats.forEach((stat) => {
        const text = stat.textContent.trim();
        const isDollar = text.includes('$');
        const numericValue = parseFloat(text.replace(/[$,]/g, ''));

        if (!isNaN(numericValue) && numericValue > 0) {
          stat.textContent = isDollar ? '$0.00' : '0';
          animateValue(stat, 0, numericValue, 2000, isDollar);
        }
      });
    }

    // Use Intersection Observer to trigger animation when stats section is visible
    const statsSection = document.querySelector('.stats-section');
    if (statsSection) {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting && !hasAnimated) {
            hasAnimated = true;
            startAnimation();
            observer.disconnect();
          }
        });
      }, {
        threshold: 0.3
      });

      observer.observe(statsSection);
    }
  });
</script>

<%- include('partials/footer-scripts') %>