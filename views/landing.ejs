<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light p-4">

<div class="container">
  <h1 class="mb-4">Home</h1>

  <% if (user) { %>
    <p>
      Welcome, <strong><%= user.username %></strong>
      (<%= user.role === 'A' ? 'Admin' : 'User' %>)
    </p>

    <a href="/users" class="btn btn-primary me-2">Users</a>
    <a href="/video" class="btn btn-warning me-2">Watch Video</a>
    <a href="/auth/logout" class="btn btn-danger">Logout</a>

  <% } else { %>

    <p>You are not logged in.</p>

    <a href="/auth/login" class="btn btn-primary me-2">Login</a>
    <a href="/video" class="btn btn-warning">Watch Video</a>

  <% } %>
</div>

</body>
</html>
