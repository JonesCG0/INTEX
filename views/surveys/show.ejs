<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Survey Details</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container">
    <h1>Survey Details</h1>

    <div class="details">
      <p><strong>ID:</strong> <%= survey.surveyid %></p>
      <p><strong>Satisfaction Score:</strong> <%= survey.surveysatisfactionscore || '—' %></p>
      <p><strong>Usefulness Score:</strong> <%= survey.surveyusefulnessscore || '—' %></p>
      <p><strong>Instructor Score:</strong> <%= survey.surveyinstructorscore || '—' %></p>
      <p><strong>Recommendation Score:</strong> <%= survey.surveyrecommendationscore || '—' %></p>
      <p><strong>Overall Score:</strong> <%= survey.surveyoverallscore || '—' %></p>
      <p><strong>Submission Date:</strong> <%= survey.surveysubmissiondate || '—' %></p>
      <p><strong>Registration ID:</strong> <%= survey.registrationid || '—' %></p>
    </div>

    <a href="/surveys" class="btn btn-secondary">Back to List</a>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
